# ============================================
# CONFIGURACIÓN DE CUENTERÍA
# ============================================
# Copie este archivo a .env y configure según su entorno

# ============================================
# CONFIGURACIÓN DEL MODELO LLM (REQUERIDO)
# ============================================
# URL del endpoint del modelo LLM
# Para gpt-oss-120b o gpt-oss-20b local
LLM_API_URL=http://localhost:8000/v1/chat/completions

# Modelo a usar
# Opciones comunes: openai/gpt-oss-120b, openai/gpt-oss-20b
LLM_MODEL=openai/gpt-oss-120b

# ============================================
# CONFIGURACIÓN DEL MODELO LLM (OPCIONAL)
# ============================================
# Temperatura para generación (0.0 - 2.0)
LLM_TEMPERATURE=0.7

# Máximo de tokens a generar
LLM_MAX_TOKENS=20000

# Timeout en segundos para llamadas al LLM
LLM_TIMEOUT=900

# Intentos de reintento si falla
LLM_RETRY_ATTEMPTS=3

# Delay entre reintentos (segundos)
LLM_RETRY_DELAY=2

# ============================================
# CONFIGURACIÓN DE LA API (OPCIONAL)
# ============================================
# Host donde escuchar (0.0.0.0 para todas las interfaces)
API_HOST=0.0.0.0

# Puerto de la API
API_PORT=5000

# Modo debug (True/False)
API_DEBUG=False

# Orígenes permitidos para CORS (separados por coma)
CORS_ORIGINS=https://lacuenteria.cl,http://localhost:3000

# Tamaño máximo de contenido (bytes)
MAX_CONTENT_LENGTH=16777216

# ============================================
# UMBRALES DE CALIDAD (OPCIONAL)
# ============================================
# Score mínimo de QA para aprobar (1.0 - 5.0)
MIN_QA_SCORE=4.0

# Máximo de reintentos por agente
MAX_RETRIES=2

# Delay entre reintentos de agentes (segundos)
RETRY_DELAY=5

# ============================================
# CONFIGURACIÓN DE WEBHOOKS (OPCIONAL)
# ============================================
# Timeout para webhooks (segundos)
WEBHOOK_TIMEOUT=30

# Reintentos de webhook
WEBHOOK_RETRIES=3

# Delay entre reintentos de webhook (segundos)
WEBHOOK_RETRY_DELAY=1

# ============================================
# CONFIGURACIÓN DE PROCESAMIENTO (OPCIONAL)
# ============================================
# Tiempo máximo por historia (segundos)
MAX_STORY_TIME=600

# Días para mantener historias antes de limpiar
CLEANUP_AFTER_DAYS=7

# Habilitar caché (True/False)
ENABLE_CACHING=True

# ============================================
# LOGGING (OPCIONAL)
# ============================================
# Nivel de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Archivo de log
LOG_FILE=cuenteria.log

# ============================================
# AUTENTICACIÓN SUPABASE (OPCIONAL)
# ============================================
# Solo necesario si se usan webhooks con Supabase
anon_key=